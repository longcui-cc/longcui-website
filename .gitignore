# ===================== 核心必忽略【你的核心需求】 =====================
# 依赖包目录 (重中之重，绝对不上传)
node_modules/
# npm 版本锁定文件 (绝对不上传，各环境安装自动生成即可)
package-lock.json
# yarn/pnpm 版本锁定文件(备用，防止后续换包管理器)
yarn.lock
pnpm-lock.yaml
package.json.md5

# ===================== VitePress / Vite 项目专属忽略 =====================
# 打包构建产物目录 (部署时NAS会本地打包，无需上传)
docs/.vitepress/dist/
# Vite 缓存目录
node_modules/.vite/
# VitePress 临时缓存
docs/.vitepress/.temp/
docs/.vitepress/cache/

# ===================== 本地开发环境 & 编辑器 忽略 =====================
# IDE/编辑器配置文件
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# ===================== 系统 & 日志 & 临时文件 忽略 =====================
# 系统隐藏文件(Mac/Linux)
.DS_Store
Thumbs.db
# 日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# 临时文件
*.tmp
*.bak
*.temp
# 环境变量文件(如果有，存放密钥/域名等敏感配置，绝对不上传)
.env
.env.local
.env.development.local
.env.production.local

# ===================== 构建/打包 冗余文件 =====================
dist/
build/
coverage/
lib/
es/
umd/
*.tgz

# ===================== 其他前端通用忽略项 =====================
# pnpm/yarn 缓存
.pnpm-store/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# ===================== Git 本身忽略 =====================
.gitattributes